import styled from 'styled-components';

export const VideoList = styled.ul`
    list-style: none;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    justify-content:center;
    margin: 10px 0;
`;